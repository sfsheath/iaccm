# eval/fixtures/

Sherd photographs referenced by `eval/cases/*.yaml`. For the seed case, create
`sherd_dn9_11/` and add the originals:

```
sherd_dn9_11/
  01_top.jpg       # upper face: knob, grooves, rouletted band
  02_profile.jpg   # side-on profile showing the raised/everted rim
  03_knob.jpg      # the central knob / handle
```

Keep these reasonably small. Real photos of correctly-identified sherds double as a visual
reference library over time, so it is fine to commit modestly-sized fixture images — but never
commit anything derived from copyrighted typology plates (see `../../docs/copyright.md`).
